# Лабораторная работа № 1. Классы

## Содержание
- [Общее задание](#общее-задание)
- [Вариант Demo (не для выдачи)](#вариант-demo-не-для-выдачи)
- [Вариант Figure1](#вариант-figure1)
- [Вариант Figure2](#вариант-figure2)
- [Вариант Function1](#вариант-function1)
- [Вариант Function2](#вариант-function2)
- [Вариант Function3](#вариант-function3)

## Общее задание

TODO

## Вариант Demo (не для выдачи)

Основы компьютерной игры в шахматы. Шахматные фигуры `Piece`.

TODO

## Вариант Figure1

Основы векторного редактора. Геометрические фигуры на плоскости `Figure` (вариант 1).

- Поддерживаемые виды фигур
  - Круг
  - Треугольник
  - Четырёхугольник
- Состояние
  - Цвет заливки
  - Толщина границы
  - Цвет границы
  - Вид фигуры и в зависимости от вида - её расположение и геометрия
- Функции над объектами
  - Распечатка объекта в поток вывода
  - Вычисление периметра
  - Вычисление площади

## Вариант Figure2

Основы векторного редактора. Геометрические фигуры на плоскости `Figure` (вариант 2).

- Поддерживаемые виды фигур
  - Эллипс
  - Прямоугольник (со сторонами параллельными осям координат)
  - Трапеция (с нижней и верхней стороной параллельными осям координат)
- Состояние
  - Цвет заливки
  - Толщина границы
  - Цвет границы
  - Вид фигуры и в зависимости от вида - её расположение и геометрия
- Функции над объектами
  - Распечатка объекта в поток вывода
  - Вычисление периметра
  - Вычисление площади

## Вариант Function1

Основы системы компьютерной алгебры. Математические функции от одного аргумента `Function` (вариант 1).

- Поддерживаемые виды математических функций
  - Константная функция $f(x) = a$
  - Линейная функция $f(x) = ax + b$
  - Квадратичная функция $f(x) = ax^2 + bx + c$
- Состояние
  - Вид математической функции и в зависимости от вида - её параметры
- Функции над объектами
  - Распечатка объекта в поток вывода
  - Вычисление значения функции от заданного аргумента $x$
  - Получение производной функции
  - Получение первообразной функции (в возможных пределах реализуемых видов функций)
  - Получение новой функции из двух существующих путём сложения
  
## Вариант Function2

Основы системы компьютерной алгебры. Математические функции от одного аргумента `Function` (вариант 2).

- Поддерживаемые виды математических функций
  - Степенная функция $f(x) = ax^b$
  - Натуральный логарифм от модуля $f(x) = c \ln|x|$
- Состояние
  - Вид математической функции и в зависимости от вида - её параметры
- Функции над объектами
  - Распечатка объекта в поток вывода
  - Вычисление значения функции от заданного аргумента $x$
  - Получение производной функции
  - Получение первообразной функции (в возможных пределах реализуемых видов функций)

## Вариант Function3

Основы системы компьютерной алгебры. Математические функции от одного аргумента `Function` (вариант 3).

- Поддерживаемые виды математических функций
  - Квадратичная функция $f(x) = ax^2 + bx + c$
  - Гармоническая функция $f(x) = a \cos(\omega x + \phi)$
- Состояние
  - Вид математической функции и в зависимости от вида - её параметры
- Функции над объектами
  - Распечатка объекта в поток вывода
  - Вычисление значения функции от заданного аргумента $x$
  - Получение производной функции
  - Получение первообразной функции (в возможных пределах реализуемых видов функций)
  
